#include <vector> // Include vector library

using namespace std;
using Vector = vector<double>; // Define vector class for easier use

double inner_product(Vector u, Vector v); // Define all the functions for basic vector operations: Adding, subtracting vectors etc.
Vector scalar_multiply( Vector u, double scalar);
Vector vector_subtraction( Vector u, Vector v);
Vector vector_add(Vector u, Vector v);
